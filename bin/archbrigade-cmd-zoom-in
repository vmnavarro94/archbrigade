#!/bin/bash

# Increase display zoom by 1 step.

CURRENT=$(hyprctl getoption cursor:zoom_factor -j 2>/dev/null | jq '.float // 1')
NEW=$(echo "$CURRENT + 1" | bc)
hyprctl keyword cursor:zoom_factor "$NEW"

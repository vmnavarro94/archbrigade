#!/bin/bash

sudo cp ~/.local/share/archbrigade/default/plymouth/* /usr/share/plymouth/themes/archbrigade/
sudo plymouth-set-default-theme archbrigade

if command -v limine-mkinitcpio &>/dev/null; then
  sudo limine-mkinitcpio
else
  sudo mkinitcpio -P
fi
